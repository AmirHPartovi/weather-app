import createTheme from '@mui/material/styles/createTheme'


const theme = createTheme({
    typography: {
      fontFamily: ['iarnsans',
      
      ].join(','),
    },
  });

  export default theme